<%= form_with(model: blind_date) do |form| %>
  <% if blind_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blind_date.errors.count, "error") %> prohibited this blind date from being saved:</h2>

      <ul>
        <% blind_date.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, start_year: Time.now.year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
